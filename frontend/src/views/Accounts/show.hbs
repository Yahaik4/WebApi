<div class="container mt-5"></div>
    {{!-- <div class="row tm-content-row">
        <div class="col-12 tm-block-col">
            <div class="tm-bg-primary-dark tm-block tm-block-h-auto">
                <h2 class="tm-block-title">List of Accounts</h2>
                <p class="text-white">Accounts</p>
                <select class="custom-select">
                <option value="" hidden>Select account</option>
                <option value="1">Admin</option>
                <option value="2">Editor</option>
                <option value="3">Merchant</option>
                <option value="4">Customer</option>
                </select>
            </div>
        </div>
    </div> --}}

    <div class="row tm-content-row">
        <div class="col-sm-12 col-md-12 col-lg-8 col-xl-12 tm-block-col">
          <div class="tm-bg-primary-dark tm-block tm-block-products">
            <div class="tm-product-table-container">
              <table class="table table-hover tm-table-small tm-product-table">
                <thead>
                  <tr>
                    <th scope="col">STT</th>
                    <th scope="col">NAME</th>
                    <th scope="col">EMAIL</th>
                    <th scope="col">PHONE NUMBER</th>
                    <th scope="col">EXPIRE DATE</th>
                    {{!-- <th scope="col" colspan="2" class="text-center">BEHAVIOR</th> --}}
                  </tr>
                </thead>
                <tbody>
                    {{#each users}}
                    <tr>
                        <td>{{sum @index 1}}</td>
                        <td class="tm-product-name">{{this.name}}</td>
                        <td>{{this.email}}</td>
                        <td>{{this.phoneNumber}}</td>
                        <td>{{this.createdAt}}</td>
                        {{!-- <td>
                            <a href="/product/edit/{{this._id}}" class="tm-product-edit-link" data-id="{{this._id}}">
                                <i class="far fa-edit tm-product-delete-icon"></i>
                            </a>
                        </td>
                        <td>
                            <a href="/product/{{this._id}}" class="tm-product-delete-link" data-id="{{this._id}}" data-toggle="modal" data-target="#exampleModal">
                                <i class="far fa-trash-alt tm-product-delete-icon"></i>
                            </a>
                        </td> --}}
                    </tr>
                    {{/each}}       
                </tbody>
              </table>
            </div>
            <!-- table container -->
            {{!-- <a
              href="add-product.html"
              class="btn btn-primary btn-block text-uppercase mb-3">Add new product</a>
            <button class="btn btn-primary btn-block text-uppercase">
              Delete selected products
            </button> --}}
          </div>
        </div>
    </div>
</div>